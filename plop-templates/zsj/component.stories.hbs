import React from "react";
import { ComponentStory, ComponentMeta } from "@storybook/react";
import {{compName}} from "./index";

export default {
  title: "{{compName}}",
  component: {{compName}},
} as ComponentMeta<typeof {{compName}}>;
const Template: ComponentStory<typeof {{compName}}> = (args) => (
  <{{compName}} {...args}>{args.children}</{{compName}}>
);
export const Defatul{{compName}} = Template.bind({});
// 传入属性
Defatul{{compName}}.args = {
};
